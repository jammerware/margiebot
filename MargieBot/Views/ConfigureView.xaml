<UserControl 
    x:Class="MargieBot.Views.ConfigureView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" >
    <StackPanel>
        <TextBlock Style="{StaticResource FieldLabelStyle}">slack key</TextBlock>
        <TextBox
            Margin="0,0,0,20"
            Text="{Binding AuthKeySlack, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

        <TextBlock Style="{StaticResource FieldLabelStyle}">wunderground key</TextBlock>
        <TextBox Text="{Binding AuthKeyWunderground, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
    </StackPanel>
</UserControl>
